# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
dist/
build/
.pytest_cache/

# Output / generated data directories (article dumps, exports) — large, regenerable
outputs/
dump/
output/
last_week/

# Local Claude Code settings + memory/plans (machine-specific, not repo content)
.claude/settings.local.json
.claude/*.lock
.claude/scheduled_tasks.lock
.claude/memory/
.claude/plans/
.serena/

# Embedded tracking DB (lives under outputs/, but ignore anywhere)
*.db
*.db-journal
*.db-wal

# Scratch / temp
*.tmp
/tmp/

# discover side-file (copy its products: block into config.yaml)
discovered_products.yaml
